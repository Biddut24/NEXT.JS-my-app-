import React from 'react';

const ProductDetails = ({params}) => {
    console.log(params.id);
    
    return (
        <div>
            <h1>Product Details hear</h1>
        </div>
    );
};

export default ProductDetails;